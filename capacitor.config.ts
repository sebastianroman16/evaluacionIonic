import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SebastianGonzalez207218456',
  webDir: 'www'
};

export default config;
